# 变更日志

本项目遵循 [约定式提交](https://www.conventionalcommits.org/zh-hans/) 与
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式。

## [未发布]

### 新增

- 仓库骨架：前后端目录、MIT 许可与上游归属声明、产品待办与架构文档。
- **S1-1 5 分钟窗口与回合生命周期**：窗口向下对齐 300 秒边界、剩余/已过秒数、
  相邻窗口推导；回合与注单的单向状态机；`isRoundTradable` 同时校验状态与窗口，
  拦截「旧回合仍挂 OPEN」时按已知结果套现。
- **S1-2 Polymarket 吃单费**：`份数 × 0.07 × p × (1 − p)`，整体一次舍入到 4 位小数；
  配套定点舍入模块（字符串最短往返串 + HALF_UP/DOWN，规避浮点尾差与二次舍入）。
- **S1-3 盘口报价与合约换算**：空档位（买一 0 / 卖一 1）等价于无报价；超龄报价判不新鲜；
  买入试算 `份数 = 金额 ÷ 卖一价` 向下取整、成本舍回金额以内、手续费在金额之外；
  卖出试算按买一价成交扣费；`estimateContracts` 供前端展示预计收益。
- **S1-4 买入下单**：SQLite 建表与回合/注单/账户仓储；买入扣 `成本 + 手续费` 并落 `ACTIVE` 注单，
  扣款与落单同事务；余额不足用带条件的原子 UPDATE 拦截（无先查后扣竞态）；
  拒绝回合未开盘、已封盘、盘口缺失或超龄的下单。
- **S1-5 卖出与部分卖出**：按买一价成交扣费并回款；整单转 `SOLD`，部分卖出拆出一笔
  `SOLD` 记录（成本按比例 4 位小数拆分）、原单继续持有剩余份数；仅当前未封盘回合可卖。
- **S1-6 回合结算与作废退款**：收盘 TWAP ≥ 开盘 TWAP 判 UP（相等算 UP）；整回合一个事务，
  回合定盘、注单改状态、批量派彩同生共死，`casSettle` 作幂等边界；缺价时按
  「有注单等 1 小时、无注单等 2 个窗口」的等待策略作废并只退本金；补结算巡检跳过
  合法停在 `LOCKED` 的上一窗口。
- **S1-7 预测盈亏统计**：已实现盈亏（赢单派彩／卖出到手 − 成本）、未实现盈亏
  （仅当前窗口按买一价重估，其余按成本挂账，无买一价记 0）、累计成本、胜率
  （赢 ÷ 已了结，作废退本金计入分母但盈亏记 0）、账户余额与总权益。
- **S1-8 REST API 与实时推送**：8 个预测端点（当前回合／盘口／下单／卖出／我的注单／
  往期回合／盈亏／成交流）+ 价格历史 + 健康检查；领域错误码统一映射 HTTP 状态
  （校验 400、未找到 404、状态冲突 409、上游不可用 503）；SSE 事件流首帧推当前状态，
  之后推 `round`/`market`/`activity`，带心跳并随连接关闭清理订阅；调度器每秒巡检
  （开回合→封盘→定盘→补结算），全部走幂等 CAS。
- **S1-9 Polymarket 行情接入**：Gamma 事件按 `outcomes` 与 `clobTokenIds` 的**下标对应**
  解析 UP/DOWN token（token 是 70+ 位数字串，全程按字符串处理不做数值转换）；
  `crypto-price` 取 60 秒 TWAP 开收盘价（`completed` 才认收盘价）；
  CLOB 账本取买一卖一（档位顺序无关、空档位回 null）；HTTP `Date` 头作时钟基准；
  可注入 fetch 与定时器，另附本地闭式模拟行情（离线可跑通全链路）；
  上游连续失败可自动降级，降级时**不写盘口**（不拿模拟价顶替真实成交价）。
- **修复（真实上游实测发现）**：回合由调度器建、目标价由行情来，谁先到都可能，
  原先只认插入那一次会导致整个窗口目标价为空；新增 `syncTargetPrice` 回填
  （仅在 `OPEN` 期间，重复同值不广播）。同时统一 SSE 首帧与行情推送的盘口形状，
  并让下单接口记录 `x-username`（成交流不再显示空名）。
- **S2-1 前端骨架与终端浅色主题**：Vite + React 19 + TS，`npm run build` 产出
  230 KB / gzip 72 KB；白色金融终端配色（白纸墨线 + 橙色强调、涨绿跌红、等宽数字
  `tabular-nums`）；类型化 API 客户端（含 `ApiError` 错误码与 `retryable` 判定、
  SSE 订阅含假 EventSource 注入点）；格式化工具（美分换算、倒计时、UTC→本地时间、
  空值统一占位）；`/api` 开发代理；vitest + Testing Library 跑通。
  注：宿主 `NODE_ENV=production` 会让 React 走生产构建、丢掉测试要用的 `act`，
  已在 vitest 配置里钉住 `NODE_ENV=test`。
- **S2-2 回合倒计时与赔率盘口**：`usePredictionMarket` 订阅中心（SSE 推回合/盘口/成交流，
  窗口切换补拉 REST，价格序列轮询对齐）；倒计时按**服务端校准时钟**自算，
  每秒心跳只负责触发重渲染；盘口超龄标记（超龄时后端会拒单）；
  赔率盘口按美分显示买卖一与隐含概率；手绘 SVG 价格曲线（含目标价虚线参照）；
  回合面板给出目标价、距目标价差值/百分比与回合进度条。
  期间修掉两个真实缺陷：盘口新旧判定把「服务端时钟超前」误判为过期；
  倒计时读的是每秒采样一次的快照，会虚高一秒（改为读当前校准时钟）。
- **S2-3 下注面板与手续费预览**：新增后端 `GET /api/prediction/preview`（不落库不扣款），
  前端的份数/成本/手续费/共扣/回款**一律取自后端**，不在前端重写费率公式——
  两处各算一次早晚会不一致，而不一致的费率就是错的钱；方向切换、金额预置档、
  上限按钮（按最坏 7% 手续费留额度，修掉 `10.7/1.07=9.999…` 的尾差）、
  250ms 防抖试算、封盘/超龄/金额非法时锁住下单并说明原因、错误码→中文提示。
- **S2-4 我的持仓与往期回合**：持仓表列出方向/份数/均价/成本/当前估值/盈亏/状态，
  仅**当前未封盘回合**的持仓可卖（其余显示「非本回合」），卖出走半仓部分卖出并回执到手金额；
  往期回合把「目标价 → 结算价」并排放，用户一眼能核对判涨跌的依据；分页控件单页时隐藏。
- **S2-5 实时成交流与盈亏统计**：成交流把 SSE 实时与 REST 历史两路按内容去重合并
  （REST 回包晚于推送时会重复，不去重就会出现两笔一模一样的成交）、相对时间、
  用户名打码、空名显示匿名；盈亏卡片把「已实现」与「未实现」分开摆
  （混成一个总盈亏会让人以为钱已经到手），并给出累计投入、持仓成本/现值、
  钱包余额、总权益与猜对/猜错/卖出/作废战绩。

### 交付

- **S3-1 Docker 部署与文档**：`docker compose up --build` 一键起前后端
  （后端 Node 24 运行时镜像无需编译——类型擦除直接跑 .ts；前端多段构建后由 nginx 托管）；
  nginx 反代 `/api` 并**单独关掉 SSE 路径的缓冲**；后端用 node 自带 fetch 做健康检查，
  前端 `depends_on: service_healthy`；数据落具名卷；`MARKET_FEED` 可选真盘/模拟/自动降级。
  补充 `docs/attribution.md` 逐项列明沿用的上游业务语义与未沿用的部分。

