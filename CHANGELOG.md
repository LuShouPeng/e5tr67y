# 变更日志

本项目遵循 [约定式提交](https://www.conventionalcommits.org/zh-hans/) 与
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 格式。

## [未发布]

### 新增

- 仓库骨架：前后端目录、MIT 许可与上游归属声明、产品待办与架构文档。
- **S1-1 5 分钟窗口与回合生命周期**：窗口向下对齐 300 秒边界、剩余/已过秒数、
  相邻窗口推导；回合与注单的单向状态机；`isRoundTradable` 同时校验状态与窗口，
  拦截「旧回合仍挂 OPEN」时按已知结果套现。
- **S1-2 Polymarket 吃单费**：`份数 × 0.07 × p × (1 − p)`，整体一次舍入到 4 位小数；
  配套定点舍入模块（字符串最短往返串 + HALF_UP/DOWN，规避浮点尾差与二次舍入）。
- **S1-3 盘口报价与合约换算**：空档位（买一 0 / 卖一 1）等价于无报价；超龄报价判不新鲜；
  买入试算 `份数 = 金额 ÷ 卖一价` 向下取整、成本舍回金额以内、手续费在金额之外；
  卖出试算按买一价成交扣费；`estimateContracts` 供前端展示预计收益。
- **S1-4 买入下单**：SQLite 建表与回合/注单/账户仓储；买入扣 `成本 + 手续费` 并落 `ACTIVE` 注单，
  扣款与落单同事务；余额不足用带条件的原子 UPDATE 拦截（无先查后扣竞态）；
  拒绝回合未开盘、已封盘、盘口缺失或超龄的下单。
- **S1-5 卖出与部分卖出**：按买一价成交扣费并回款；整单转 `SOLD`，部分卖出拆出一笔
  `SOLD` 记录（成本按比例 4 位小数拆分）、原单继续持有剩余份数；仅当前未封盘回合可卖。
- **S1-6 回合结算与作废退款**：收盘 TWAP ≥ 开盘 TWAP 判 UP（相等算 UP）；整回合一个事务，
  回合定盘、注单改状态、批量派彩同生共死，`casSettle` 作幂等边界；缺价时按
  「有注单等 1 小时、无注单等 2 个窗口」的等待策略作废并只退本金；补结算巡检跳过
  合法停在 `LOCKED` 的上一窗口。
- **S1-7 预测盈亏统计**：已实现盈亏（赢单派彩／卖出到手 − 成本）、未实现盈亏
  （仅当前窗口按买一价重估，其余按成本挂账，无买一价记 0）、累计成本、胜率
  （赢 ÷ 已了结，作废退本金计入分母但盈亏记 0）、账户余额与总权益。
- **S1-8 REST API 与实时推送**：8 个预测端点（当前回合／盘口／下单／卖出／我的注单／
  往期回合／盈亏／成交流）+ 价格历史 + 健康检查；领域错误码统一映射 HTTP 状态
  （校验 400、未找到 404、状态冲突 409、上游不可用 503）；SSE 事件流首帧推当前状态，
  之后推 `round`/`market`/`activity`，带心跳并随连接关闭清理订阅；调度器每秒巡检
  （开回合→封盘→定盘→补结算），全部走幂等 CAS。

